function theBeatlesPlay(muiscians, instruments) {
  const array = []
  for (let i = 0, l = musicians.length; i < l; i++) {
    array.push(`${muiscians[i]} plays ${muiscians[i]}`)
  }
  return array
}
